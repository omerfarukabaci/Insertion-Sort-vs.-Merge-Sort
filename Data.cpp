#include "Data.h"

void Data::set_line(string inputLine) {
	line = inputLine;
}

void Data::set_timeStamp(string inputTimeStamp) {
	timeStamp = inputTimeStamp;
}

void Data::set_lastPrice(float inputLastPrice) {
	lastPrice = inputLastPrice;
}